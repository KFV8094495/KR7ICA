@model IEnumerable<ThAmCo.Events.Data.StaffBookings>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Staff)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attended)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Staff.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attended)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.StaffId, eventId = item.EventId }) |
                @Html.ActionLink("Details", "Details", new { id = item.StaffId, eventId = item.EventId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.StaffId, eventId = item.EventId })
            </td>
        </tr>
}
    </tbody>
</table>
